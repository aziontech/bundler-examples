version: 2
updates:
- package-ecosystem: npm
  directory: "/"
  schedule:
    interval: daily
  # Wait a week before proposing a freshly published version, so a
  # compromised release has time to be pulled before we adopt it.
  cooldown:
    default-days: 7
  open-pull-requests-limit: 20
